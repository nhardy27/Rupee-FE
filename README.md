
  # Modern Admin Panel UI

  This is a code bundle for Modern Admin Panel UI. The original project is available at https://www.figma.com/design/MMsxIoCk0VvW301am09ixs/Modern-Admin-Panel-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  